server:
  port: ${PORT:8080}

spring:
  application:
    name: qlbh



#  datasource:
#      url: jdbc:postgresql://localhost:5432/QLBH_DB
#      username: postgres
#      password: 123456
#      driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

